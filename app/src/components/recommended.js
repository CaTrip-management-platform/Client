import { View, Text, ScrollView } from "react-native";
import React from "react";

const Recommended = () => {
  return (
    <View>
      <ScrollView>
        <Text>test</Text>
      </ScrollView>
    </View>
  );
};

export default Recommended;
