import { View, Text } from "react-native";
import React from "react";

const DetailActivity = ({ route }) => {
  return (
    <View>
      <Text>DetailActivity</Text>
    </View>
  );
};

export default DetailActivity;
